import {readFile,writeFile,mkdir} from 'node:fs/promises';
import {build} from 'esbuild';
import {chromium} from 'playwright';
let source=await readFile('competition/game.js','utf8');source=source.replace(/\n}\s*$/,`\nglobalThis.towerReview={showcase,d,camera,rig,scene,renderer,gun,towerStone,towerMesh};\n}`);
const bundled=(await build({stdin:{contents:source,resolveDir:process.cwd()+'/competition',loader:'js'},bundle:true,write:false,format:'esm'})).outputFiles[0].text;
const html=(await readFile('competition/index.html','utf8')).replace('<script type=module src=game.js></script>',`<script type=module>${bundled}</script>`);
const b=await chromium.launch({channel:'chrome'});try{const p=await b.newPage({viewport:{width:1200,height:800}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route('**/tower-review',r=>r.fulfill({body:html,contentType:'text/html'}));await p.goto('http://127.0.0.1:4173/tower-review');await p.locator('#s').click();await p.waitForTimeout(300);await mkdir('evidence/showcase',{recursive:true});await p.screenshot({path:'evidence/showcase/locked.png'});await p.evaluate(()=>towerReview.d.cleared=2);await p.waitForTimeout(250);await p.screenshot({path:'evidence/showcase/wave3.png'});await p.evaluate(()=>towerReview.d.cleared=4);await p.waitForTimeout(250);await p.screenshot({path:'evidence/showcase/wave5.png'});await p.setViewportSize({width:390,height:844});await p.waitForTimeout(250);await p.screenshot({path:'evidence/showcase/phone.png'});console.log({errors,render:await p.evaluate(()=>towerReview.renderer.info.render)});}finally{await b.close();}
