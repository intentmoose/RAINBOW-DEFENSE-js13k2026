# Decisions — updated 2026-09-13 Jerusalem

## 2026-09-13 — semantic procedural audio

Give actions distinct envelopes and pitches; keep tower attacks quiet and batch simultaneous recovery rewards. Use no sampled assets. Preserve shot cadence and manual VR reload. Fix pitch at -16 degrees with no adjustment, per the concurrent user request. Deterministic minification and20 fixed packing selectors fund the change; measure decoder startup separately. See docs/AUDIO.md.

## 2026-09-13 — closed unicorn surfaces

Repair the canonical loft generator with closed end caps, preserving all original vertices/wall faces. Match its two-sided materials in the batched renderer, reuse mane material for wings, and remove the overlapping torus gun guard. No anatomy replacement, lower-detail model, copied arrays, auto-reload or interaction change. Canonical pin eb8ff13121219f617dffb66716c213aee5c6f37b. Measured gameZIP13,307bytes; the two extra bytes fund actual hole closure. See docs/SOLID_MODELS.md.

## 2026-09-13 — model-led TD interface

Follow the user's explicit physical-only VR reload request: no face-button/support-trigger assistance; desktop/touch assistance remains. Choose actual tower/gun models on persistent side cards, start with one type each, expose the locked wave3/wave5 equipment throughout play, and keep spending deliberate through local actions. Pistol is semi-automatic, one charge per press; other weapons retain their cadence. Replace the pouch with available magazine geometry. Fixed pads, six-wave progression, unlimited magazine supply and the two canonical tower families remain. No additional AR/catalogue or magnifying optic was added. See TD_UX.md for tests and measured ZIP cost.

## 2026-09-13 — submission playtest and keyboard focus fix

The user requested submission playtesting and repairs. Space after a mouse reload could activate the focused reload button again on release; active gameplay now consumes Space/Enter native button activation. Chrome/Firefox regression and a unit test cover the fix. Instruction wording is shared to recover ZIP bytes; the world board retains transient feedback and its next action. Fixed packing rate1650, unchanged memory settings/anatomy/gameplay; archive13,310bytes, SHA256 caba602b67aefafc27f1e17c48436e10ce08cfc1f6dba29f3d06409995b3d53f. All game tests now participate in test:13k and CI through test:game, excluding only the separate review Worker test. CI source-access failure is not bypassed. See SUBMISSION_PLAYTEST.md for evidence and remaining physical/public-source checks.


## 2026-09-13 — contextual UX and the correct tower source

User rejected the decorative unlock signs, crowded controls and solid sniper scope, requested contextual tower menus and a wave-complete celebration, and supplied the Tiny Unicorn Lab shared chat as the tower reference. Confirmed that chat refers to architectural towers in intentmoose/tiny-unicorn-lab PR4, not the Kenney kit previously inspected. Updated the consumer submodule to17ec7aee234dc4a6d43904a35375a33d9b02d97c; src/tiny-unicorn.js is unchanged against24c889d.

Implemented a separate right-side armory (world panel in VR; independent scrolling panel on desktop/touch). Aiming at a pad/tower selects it; its nearby, camera-facing menu offers build/upgrade, empty-pad type selection and start-wave. Menus scale with viewing distance; the central mission panel yields while a tower menu is selected. Spending requires a deliberate menu click/trigger; selection itself is free. Three-second wave-complete celebration shows bonus points/unlocks with particles, chord and haptics, blocks an early next wave, and preserves explicit start. Final victory waits for that celebration. Sniper now has an open tubular optic and unobstructed reticle sightline, seated against its receiver. It is not a magnifying scope; physical headset alignment/comfort remain unverified.

Current artifact13312 /13312 bytes, no spare. Build packs the interface HTML/CSS together with strict game source; a dark Loading message and official engine reference remain in root HTML. Fixed Roadroller parameters remain deterministic (128MiB request,97.27MiB reported model arrays); exact reconstruction and separate-process ZIP reproduction are required. Tower recipes are extracted at build time from pinned canonical Prism/Frost tier1 compositions; engine Lathe/Box/Torus primitives replace equivalent library primitives and transforms round to.001. A guard rejects changed upstream source, and a geometry test retains canonical vertex positions within.003 local units. Six cached pad models contain both cached family forms; upgrades grow them16% per level, not the catalogue’s higher-tier branches. No copied anatomy, external model, second runtime or catalogue UI. To fund the UX: decorative showcase clones, duplicate global pad-price text, small gun ribs, per-spire hues/tilt, core pulsing and progressive fog/rainbow changes were removed. The twilight shader, brighter fixed rainbow, instanced unicorns, guns/reload, fixed pads, economy and six-wave campaign remain.


## 2026-09-13 — visible rewards and placement

User requested visible side displays of future guns/towers to motivate progression and asked about free placement. Added world-space wave 3 shotgun + slow-tower and wave 5 sniper previews; labels turn green when actually unlocked and reset on replay. Preview guns reuse the real gun geometry with independent transforms; two sign textures are cached. Portrait layout narrows the displays. Fixed pads remain: fast VR selection, readable lanes and balanced coverage; unrestricted placement is not implemented. The six-wave game, reload, economy, anatomy pin and actual unlock rules are preserved.

Artifact: 13,300 /13,312 bytes,12 spare,one byte below version18. SHA256 35f352ffa8aa6d1ebd649ef5a7f5fa847ab6eac69e38ebe0b82c06a35d88120e. Fixed Roadroller parameters now use16 explicit selectors with maxMemoryMB128 (reported model arrays97.27MiB). Exact strict reconstruction and separate-process ZIP reproduction pass. Desktop decoder medians: Chrome1,310.8ms versus761.8ms; Firefox1,093ms versus564ms. Roughly0.53–0.55 seconds extra initial unpacking; engine/network excluded. This is a measured runtime tradeoff, not the competition limit. Physical Quest startup/performance and phone/full human campaign checks remain open. Explicit selectors replace the prior packing parameters; no runtime optimizer or new external asset.


28. User approved supporting-side hip-pouch supply. Use a generous, height-relative, mirrored VR grab location and reuse existing geometry. Fresh ammo comes from the pouch or assisted reload; manual reseating preserves remaining ammo, superseding the replenishable-same-cartridge detail in decision2. Reuse one visual discarded shell with world pose/velocity; preserve interactive current-magazine throws and all reload safety. Ammo stays unlimited; tower cleanses still pay points. Tower-linked ammo was a design question and is not implemented. No assault rifle added; existing shotgun/sniper unlocks remain. ZIP13,302 bytes,10 reserve;55 tests and browser/touch/IWER pass. See docs/HIP_POUCH.md.

24. User requested a whole-game pass including physical magazine throws. Removed magazines live in world space, inherit hand motion and use analytic gravity/bounce without a physics engine. Pick up/reinsert or recall via assisted reload. Ready-gun pistol-grip proximity allows deliberate handover, superseding decision 3's no-support-handover restriction. Preserve authoritative ammunition, partial-pull and held-trigger safeguards.
25. Improve the established game: distinct gun shapes/barrel-correct shotgun beams, coordinated canonical unicorn reactions, animated towers, twilight/ground shader, clearer valley composition and higher-resolution HUD. Runtime data size alone does not veto quality. Keep anatomy pin and useful instancing; physical performance remains separate.
26. Supersede decision 4's no-runtime-unpacker choice with exact-pinned Roadroller 2.1.0, fixed parameters, strict text reconstruction and disabled free-variable pollution. Decoder/data are inside the ZIP, with no runtime WebAssembly or new external resource. Skip optimizer API entirely: optimize(0) retunes randomly in this pinned version. Every build round-trips before emission; tests rebuild in a separate process. Final 13,025 bytes / 287 reserve versus 14,069 without packing. Accept desktop median startup overhead about 0.43–0.64 seconds, with physical Quest startup still a release check. No RAM-savings claim.
27. Teach the next action: loaded start, objective/build/start/fire sequence, centered initial VR purchase, trigger/pad guidance, part glow/grip hints, empty-energy reload priority and no wave start during unfinished reload. Keep -6-degree default pitch and adjustment. Evidence/limits: docs/WHOLE_GAME_PASS.md. The numbered historical decisions follow.

1. Keep Rainbow Herd primary and AFRAME.THREE architecture. Engine availability currently works; no reason to migrate. Tiny Unicorn Lab remains pinned.
2. One replenishable cartridge and separate deliberate prime.12-second beam capacity supports several recoveries;1.3-second assisted choreography shares all rules. No ammunition scarcity or invented pet system.
3. Stable handed roles from inputSource.handedness; H or B/Y explicitly swaps. Supporting grip cannot mount or steal aim. Analytic grip-local manipulation, no physics/asset/framework import from Spillway.
4. Single root HTML, Terser5.47.1 and build-only Zopfli1.0.15 via fflate ZIP extension. This shares compression across HTML/JS and fits the requested interaction. Zopfli's build-tool WebAssembly never enters the game; no runtime unpacker/eval. Fail size gate before overwriting known candidate; oversized experiment goes to ignored work/oversize.zip.
5. Remove ambient clouds/music, decorative reticle fill and unused ride-pose helper before cutting gameplay or transition safeguards. Mechanical/conversion/beam sounds retained. Only additional game fix is full-pool Alicorn arrival; UI framing/input-cancellation support weapon integration.
6. Preserve48-unit/64-projectile/240-spark bounds. At full pool, boss promotes an unmounted existing slot and keeps seen history. No absent-boss state, mount replacement or extra allocation.
7. Original freeze at13,220 bytes; superseded by the explicitly requested optimization audit below. Physical ergonomics, sustained horde balance and Quest performance remain explicit human gates. No visibility change, terms acceptance or submission without authorization.
8. On the user's explicit "please deploy" request, publish the tested candidate to the existing owner-private Site. Preserve divergent e04cd7d in its pushed archive branch; join its ancestry with an ours-strategy merge retaining the candidate tree. Use ordinary fast-forward pushes, no force push. Deployment succeeded as version 7 from ea20fce669e2c5f71e8c41dbc1ab4c2512a670fa. This authorizes replacing the private preview, not competition submission or source visibility changes.

9. Optimize the existing game without making the debated tower-defense redesign. Preserve anatomy, gameplay pools and physical interaction. Compact render-only indices, normalized byte colors, unused-attribute removal, one context and analytic contact shadows trade 37 fewer ZIP bytes for 16,596 fewer retained array bytes. Use tested private weapon-property mangling and discard unused frozen literals; never rename external APIs or coerce booleans. Final 13,183 bytes, 129 reserve. Keep the full engine and fixed resolution cap pending physical profiling.

10. On the explicit combined-game build request, replace persistent allied-herd/riding/flight/draft with points-funded tower defense; this supersedes decisions1,3,5,6,9 where mechanics conflict. Preserve original on pushed codex/legacy-herd-bd4319a with exact baseline ZIP. One game/simulation; no second engine or imported Prism runtime.
11. Six-wave campaign unlocks endless; weapon unlocks follow completed waves2/4. Shared currency for six pads and gun upgrades, maxlevel3. Towers support active shooting: greedy towers-only reference loseswave4. No per-wave spawned objects or pathfinding.
12. Controller-attached procedural gloves instead of external default hand models or optical tracking. Canonical horn reused for towers, all pads share geometry. Default XR gun pitch12degrees, adjustable6degrees, physical barrel-aligned aim. Retain authoritative reload safeguards.
13. Accept one384x160 no-mipmap texture shared across immersive world/gun HUDs for usable VR economy/reload controls. Geometry/instance payload decreases4.3%, but total memory reduction is not claimed due to that texture. Final candidate13,279bytes,33 reserve; real build/35 tests and browser/IWER/touch checks pass. Physical hardware remains open.

14. For the requested seated controls, keep physical turning and add left-stick translation/right-stick30-degree snap turn with neutral rearm and headset-pivot correction. A single controller gets forward/back plus turn. Roles depend on handedness, independently of gun ownership.
15. Match enemy pool to existing40-enemy wave maximum; one boss allowsN+2 horns. Use native half-float creature matrices when available with full-float fallback. Preserve full-precision simulation, gun and hand interaction. Share octahedron geometry across scenery/tower heads/core/emitter; replace duplicate eye glows and simplify unlit geometry. Do not modify canonical anatomy.
16. The crystal-valley visual/controls release retains56,364bytes of geometry/instance arrays versus104,334, with unchangedHUDtexture. Report scoped savings only. Final ZIP13,295bytes,17reserve;38tests and browser/touch/IWER checks pass. Physical comfort remains an open user test.

17. User clarified compressed ZIP priority, not RAM. Do not equate array savings with competition headroom. Engine default hands are allowed; RAM is not a reason to reject them. Procedural hands remain pending actual integration/ZIP comparison.
18. Neutral pitch, 8cm stroke, only seated/unprimed bar pickup, nearest eligible target, pull/release guidance and manipulating-hand haptics. Explicit BUILD TOWER purchases next pad; TYPE selects; tower required before START WAVE.
19. Favor guidance over optional RAM-saving code/decoration: full-float matrices, regular uploads, shared labels and audited private input-property mangling. 13,309 / 13,312 bytes, 3 free; SHA256 7f56d049c8a0f7b1c957690813100513e38f67e824e56e8fec64718452ee035a; 40 tests and browser/touch/IWER checks pass. See docs/USABILITY.md.

20. Repeated user request prompted actual default-hand integration. Reuse pinned A-Frame hand-controls loading/orientation/animation through a narrow entity adapter, keeping one existing renderer and two cached models. This supersedes the procedural-glove decision; models are engine default resources, not ZIP payload. No optical tracking.
21. Default pitch -6 degrees; support contact moves from z=-.05 to z=.02 in grip space. Keep existing physical reload safeguards and show assisted fallback throughout. Main-hand squeeze no longer buys towers.
22. Guard the initial tower funds in simulation and hide early unavailable controls; center the initial immersive buy action and show purchase feedback on the board. Audited private simulation/geometry keys and compact HTML produce 13,298 / 13,312 bytes, 14 free; SHA256 31a7e5f4bc9aa47ebb2f9670f1328bd8de74274e4baa4b4d6f7ae4a2b8f1dfce. See docs/RECHECK.md.

23. The user asked to reconsider design after the RAM/ZIP confusion. Audit quality against compressed bytes and actual playability, with no arbitrary RAM target. Preserve the approved defense loop, canonical anatomy and useful instancing; reopen visual simplifications justified only by runtime data size. Eight isolated trials reproduce the current ZIP and show, among other results, a higher-resolution HUD plus smoother rainbow and simpler geometry code at 13,290 bytes (8 smaller). These are prototypes, not a release change. See docs/QUALITY_BUDGET_RETHINK.md; the next implementation should prioritize readability, scene composition and gun/tower feedback, with separate physical performance checks.

## 29. Automatic chambering supersedes manual cocking — 2026-09-13

User approved the simpler physical reload and a short button reload. Insert/release automatically chambers in120ms; replace the large top handle with a small animated side bolt. Assistance takes one second, with visible progress. Preserve held-trigger latch through the final assisted interval as well as chambering. Interruption clears input and settles a seated bolt; an absent magazine stays unready. No extra manual rack or countdown penalty. Hip ammunition remains unlimited, reseating conserves energy, towers pay points. This supersedes earlier mandatory charging-handle decisions; no weapon/progression or anatomy change.

## 30. One six-wave mode; spend the savings on tower identity — 2026-09-13

User confirmed the extra mode means ENDLESS HORDE and agreed to remove it if it benefits towers. Keep one six-wave run and replay; remove separate endless entry/unlock storage/alternate rules, superseding decision11 only for Endless. Preserve weapon unlocks and tower economy. Use compact procedural castle bases, battlements, prism cannon recoil and orbiting slowing crowns inspired by the inspected hackathon tower kit. No Kenney binary/texture import and no edits to the other game. Measured net increase23 ZIP bytes with124 spare.

## 31. Improve interaction and guidance before adding features — 2026-09-13

User requested better UX. Make the visible tower body a usable upgrade target, retain direct pads for building, and disable above-pad hit volumes when no tower exists. Clearly distinguish ammo quantity from reload progress, label gun upgrades explicitly, show six-wave progress and weapon unlocks, and use HOLD TO FIRE wording. Enlarge button text and reduce irrelevant angle-control clutter outside VR preparation. Preserve all authoritative economy/reload rules and spend measured ZIP bytes on clarity.
